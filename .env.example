# Server-side API keys (used by server/index.ts — NEVER exposed to browser)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
RENTCAST_API_KEY=your-rentcast-api-key-here

# CORS — comma-separated origins allowed to call the API (defaults to localhost in dev)
# CORS_ORIGIN=https://yourdomain.com

# Optional: Google Maps Street View (client-side)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
